# AgentCritic Framework

Multi-agent orchestration framework for codebase ingestion and automated review.

## Governance
This project follows the Agent Guardian security guidelines.

## Setup
1. Copy `.env.example` to `.env`
2. Install dependencies: `pip install -r requirements.txt`
3. Run the agent: `python main.py`
